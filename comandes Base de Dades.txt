C:\MariaDB\mariadb-12.1.2-winx64\bin



cd C:\MariaDB\mariadb-12.1.2-winx64\bin


Database

cd C:\MariaDB\mariadb-12.1.2-winx64\bin
mariadb -u root -p
password: mariadb

SELECT User, Host FROM mysql.user;

CREATE DATABASE projecte_dsa_db;

show databases;
use dsadb;
show tables;


CREATE USER 'manel'@'localhost' IDENTIFIED BY 'pass123';

CREATE DATABASE dsadb;
USE projecte_dsa_db;

CREATE TABLE `User`(
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    coins INT DEFAULT 0
);

CREATE TABLE Item (
    id INT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    durability INT NOT NULL,
    price INT NOT NULL,
    emoji VARCHAR(10),
    description VARCHAR(255),
    quantity INT NOT NULL DEFAULT 1
);

CREATE TABLE UserItems (
    user_id INT NOT NULL,
    item_id INT NOT NULL,
    quantity INT NOT NULL DEFAULT 1,

    PRIMARY KEY (user_id, item_id),

    FOREIGN KEY (user_id) REFERENCES User(id) ON DELETE CASCADE,
    FOREIGN KEY (item_id) REFERENCES Item(id) ON DELETE CASCADE
);


INSERT INTO Item (id, name, price, durability, emoji, description) VALUES
(1, 'Calculator', 200, 200, '📱', 'Solve tricky math problems with ease.'),
(2, 'Laptop', 200, 200, '💻', 'Complete reports and projects efficiently.'),
(3, 'Notebook', 150, 150, '📓', 'Keep track of class notes and ideas.'),
(4, 'Pen', 100, 100, '🖊️', 'Write down important formulas and reminders.'),
(5, 'Old Mobile', 180, 180, '☎️', 'Check messages and stay connected the old-school way.'),
(6, 'Energy Drink', 120, 120, '🥤', 'Boost your focus and stay awake during long study sessions.'),
(7, 'Headphones', 160, 160, '🎧', 'Concentrate on work and block out distractions.'),
(8, 'Backpack', 200, 200, '🎒', 'Carry all your items and tools wherever you go.'),
(9, 'USB Drive', 100, 100, '💾', 'Store and transport your important files easily.'),
(10, 'Coffee', 100, 100, '☕', 'Recharge your energy and stay productive.');



SELECT * FROM `User`;
SELECT firstName, lastName, city FROM `User`;

Per eliminar la base de dades:
DROP DATABASE projecte_dsa_db;

